
<header>
    <div class="navbar navbar-default navbar-static-top" role="navigation">
        <div class="container">
            <div class="navbar-header col-md-3 col-lg-2">
                <a class="navbar-brand" href="{{ path('index') }}">
                    Symfony Demo
                </a>

                <button type="button" class="navbar-toggle"
                        data-toggle="collapse"
                        data-target=".navbar-collapse">
                    <span class="sr-only">{{ 'menu.toggle_nav'|trans }}</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav navbar-right">

                    {% block header_navigation_links %}
                        <li>
                            <a href="{{ path('index') }}">
                                <i class="fa fa-home" aria-hidden="true"></i> {{ 'menu.homepage'|trans }}
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('blog') }}">
                                <i class="fa fa-home" aria-hidden="true"></i> {{ 'menu.blog'|trans }}
                            </a>
                        </li>

                        {% if is_granted('ROLE_ADMIN') %}
                        <li>
                            <a href="{{ path('index') }}">
                                <i class="fa fa-lock" aria-hidden="true"></i> {{ 'menu.admin'|trans }}
                            </a>
                        </li>
                        {% endif %}
                    {% endblock %}

                    <li>
                        <a href="{{ path('index') }}"> <i class="fa fa-search"></i> {{ 'menu.search'|trans }}</a>
                    </li>

                    {#                        {% if app.user is defined %}#}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" id="user">
                            <i class="fa fa-user" aria-hidden="true"></i>
                            <span class="caret"></span>
                            <span class="sr-only">Test {#{ app.user.fullname }#}</span>
                        </a>
                        <ul class="dropdown-menu user" role="menu" aria-labelledby="user">
                            <li>
                                <a href="{{ path('index') }}">
                                    <i class="fa fa-edit" aria-hidden="true"></i> {{ 'menu.user'|trans }}
                                </a>
                            </li>
                            <li class="divider"></li>
                            <li>
                                <a href="{{ path('index') }}">
                                    <i class="fa fa-sign-out" aria-hidden="true"></i> {{ 'menu.logout'|trans }}
                                </a>
                            </li>
                        </ul>
                    </li>
                    {#                        {% endif %}#}

                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" id="locales">
                            <i class="fa fa-globe" aria-hidden="true"></i>
                            <span class="caret"></span>
                            <span class="sr-only">{{ 'menu.choose_language'|trans }}</span>
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</header>